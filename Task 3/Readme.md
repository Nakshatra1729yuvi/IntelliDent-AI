Python script that checks image quality in a dataset by detecting blurriness (using Laplacian variance), low brightness, and low contrast.
Analyzes 10 images from a folder and prints a clean report with each image name and quality status (Good / Poor), including reasons for poor quality.
